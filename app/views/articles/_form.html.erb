<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading"><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</div>

      <div class="panel-body">
        <% @article.errors.full_messages.each do |msg| %>
          <div><%= msg %></div>
        <% end %>
      </div>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label :title %><br>
      <%= f.text_field :title, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= f.label :text %><br>
      <%= f.text_area :text, rows: 8, class: 'form-control' %>
    </div>

    <%= f.submit "OK", class: 'btn btn-primary' %> <%= link_to 'Cancel', articles_path, class: 'btn btn-default' %>

 <% end %>
